node_modules/
dist/
.env
*.log
.DS_Store

# Vendored OpenSCAD-WASM build for the standalone client (re-fetched by
# scripts/fetch-openscad-wasm.mjs — no npm package exists to depend on instead)
client/public/vendor/
